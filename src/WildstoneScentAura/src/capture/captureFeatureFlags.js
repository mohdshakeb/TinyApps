// Session 1 (the iOS Safari MediaRecorder spike) hasn't run yet -- no iPhone
// was available when this session was built (see Planning/CONTEXT.md,
// Session 0 sequencing note). Snapshot capture is the only guaranteed path
// until that spike sets this flag from real evidence, per the plan's
// capture architecture decision.
export const ENABLE_VIDEO_CAPTURE = false
